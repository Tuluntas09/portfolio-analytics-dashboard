# Finnhub REST API key — used only by the Node.js proxy (npm run api).
# Never sent to the browser; never bundled by Vite.
# Get a free key at https://finnhub.io
# Copy this file to .env.local and fill in your key.
FINNHUB_API_KEY=
